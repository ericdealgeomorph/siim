# Documentation build requirements (Read the Docs / fresh envs).
# The compiled 2D model stack (fastscape / fastscapelib-fortran) is NOT
# required: docs/conf.py mocks whatever is missing, and the 2D example pages are
# not executed. The pip-light runtime (numpy/scipy/numba/matplotlib) IS required
# so the 1D getting-started page can execute at build time; ipykernel provides
# the kernel MyST-NB runs it in.
sphinx>=8
furo
myst-nb
sphinxcontrib-bibtex
ipykernel
numpy
scipy
numba
matplotlib
